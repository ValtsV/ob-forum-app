import { FilterNumbersPipe } from './filter-numbers.pipe';

describe('FilterNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
